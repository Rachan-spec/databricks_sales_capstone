name: Databricks Sales Capstone

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Repository Verified
        run: echo "Repository uploaded successfully"
